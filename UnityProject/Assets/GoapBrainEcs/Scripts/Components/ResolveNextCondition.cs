using Unity.Entities;

namespace GoapBrainEcs {
    /// <summary>
    /// A tag component that marks the search to start resolving the next condition
    /// </summary>
    public struct ResolveNextCondition : IComponentData {
    }
}