namespace Common {
    /// <summary>
    /// A single interface that collects all interfaces that DataPool needs
    /// </summary>
    public interface IDataPoolItem : Identifiable, IntIdentifiable {
    }
}