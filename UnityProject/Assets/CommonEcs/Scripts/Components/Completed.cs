using Unity.Entities;

namespace CommonEcs {
    /// <summary>
    /// A common tag component that identifies an entity as completed
    /// </summary>
    public struct Completed : IComponentData {
    }
}